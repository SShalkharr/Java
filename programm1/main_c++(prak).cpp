#include <iostream>
#include <cmath>

int main(int argc, char** argv) {
	
	using namespace std;
	int x, y;
int g = gcdex (a, m, x, y);
if (g != 1)
	cout << "no solution";
else {
	x = (x % m + m) % m;
	cout << x;
}
	

	return 0;
}

